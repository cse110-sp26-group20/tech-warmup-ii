I approve the Mathematical Model and the 3-Layer Architecture.

Task: > 1. Implement Layer 1 (The Brain) in src/logic/SlotMachineMath.js. Use ES6 modules and include JSDocs with type annotations.
2. Implement Layer 2 (The Wallet) in src/state/Wallet.js using localStorage for persistence.
3. Crucial: Write a complete suite of Jest unit tests for SlotMachineMath.js in tests/SlotMachineMath.test.js. The tests must verify that the calculatePayout function correctly identifies wins for all symbols and returns 0 for a losing grid.

Ensure all code follows Clean Code principles (meaningful names, small functions)

Use the exact symbol weights and paytable you proposed in the previous response to ensure we hit that 96% RTP target.